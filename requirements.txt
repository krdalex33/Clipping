# Версии зафиксированы для воспроизводимости, кроме yt-dlp:
# yt-dlp обязательно ставим последней версией — YouTube часто меняет защиту,
# и только свежий yt-dlp продолжает работать.
aiogram==3.13.1
httpx==0.27.2
# Запасной ffmpeg+ffprobe на случай, если хостинг собрал проект не через Dockerfile
# (тогда системного ffmpeg нет). Пакет сам скачает статические бинарники.
static-ffmpeg==3.0
yt-dlp
